{
  "name": "playwright-api",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.41.0"
  }
}
